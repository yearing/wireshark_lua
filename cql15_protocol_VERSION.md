# CQL15 Protocol 插件版本说明

## 版本 2.1.0 (2026-08-07)

### 概述

重庆15号线 SIG-ISCS 接口 Wireshark Lua 解析插件。依据《CQS15A_SIG_TEC_ATS_024》V1.3 实现网络全局调度系统（SIG）与综合监控系统（ISCS）之间全部消息类型的解析。

### 功能

- 支持全部 14 种消息类型解析：
  - SIG → ISCS：01 列车运行信息及阻塞信息、02 站台信息、03 SPKS 设备状态、06 当天时刻表、18 列车联动、19 段场出入库
  - ISCS → SIG：07 SCADA 供电区段、15 客流信息、16 请求时刻表、21 区间超水位、33 过分相、37 区间瓦斯、19 FAS 火灾信息
  - 双向：08 回执、09 心跳
- 类型 19 按 TCP 方向区分：SIG 发起 = 段场出入库，ISCS 发起 = FAS 火灾信息
- CRC 累加和校验（附录1算法，低字节大写十六进制对比，显示 OK/FAIL）
- TCP 分包重组（desegment）
- 长度域合理性校验与结构长度一致性告警

### 展示

- 字段格式：`编号.名称(Nbytes),字节序(X):值 [过滤器名]`
- 循环体（多列车/多设备/多行程）偏移自动累加
- 时间域统一 19 字节（YYYY-MM-DD HH:MM:SS），NUL 分隔符不再触发 Trailing stray characters（send_time 改用 FT_BYTES）

### 过滤

- 全部字段注册 `cql15.*` ProtoField，可在显示过滤器使用，如：
  - `cql15.data_type == "02"`
  - `cql15.train_num == "15271001"`
  - `cql15.station_no == "1502"`
  - `cql15.dev_state == 0x0001`

### 枚举描述

- 30 个枚举字段自动追加中文描述，如：
  - `EB状态:0 | 未激活`、`1 | 激活`
  - `列车位置:2 | 车辆段`
  - `驾驶模式:1 | RM模式`、`3 | CM模式`
  - `供电区段:1 | 已供电`

### 变更记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| 2.1.0 | 2026-08-07 | 类型19按方向区分段场出入库/FAS；全部字段可过滤并标注过滤器名；枚举字段追加中文描述；send_time 改 FT_BYTES 消除 stray；CRC 定位改用结构解析结果；08/09 无数据域固定31字节 |
| 2.0.0 | 2026-08-07 | 依据 V1.3 实现全部消息类型；类型01新增湿轨模式(49→50字节)；全部字段带 Nbytes/字节序 标注；修复同段多包越界 |

### 端口配置

- 默认监听端口：5000（可在 解析 菜单首选项中修改，支持多端口用空格分隔）
- SIG 侧为服务端，ISCS 侧为客户端

### 环境

- Wireshark 4.x（Lua 5.2+）
- 文件名：`cql15_protocol.lua`，存放于 Wireshark 个人插件目录
