# DGL1 Protocol 插件版本说明

## 版本 1.0.3 (2026-08-10)

### 概述

东莞1号线 ATS-ISCS 接口 Wireshark Lua 解析插件，依据《东莞市城市轨道交通1号线一期工程ATS-综合监控系统接口说明书》V1.1（`dgl1_ats.md`）实现信号 ATS 系统与综合监控 ISCS 系统之间全部消息类型的解析。

本插件将 ATS-ISCS 接口的二进制应用层报文解析为结构化的字段树。报文封装在**信息帧**中（帧头/帧数/帧序/长度/帧尾），支持帧内多消息、跨帧消息重组与 TCP desegment。

---

## 一、实现的功能

### 1. 消息类型解析（9 种）

| 消息ID | 方向 | 消息 | 解析内容 |
|--------|------|------|----------|
| 0x01 | 双向 | 心跳信息 | 预留 Spare(8) |
| 0x03 | SIG→ISCS | 站台到站列车信息 | 站台数目 + 每站台：站台编号 + 每列车：车组号/表号/车次号/目的地码/预计到离时间/早晚点/到站离站扣车跳停清客末班标记/编组/驾驶员/载客属性/目的地编号/运营属性/列车属性/清客类型 |
| 0x05 | SIG→ISCS | 列车位置信息 | 列车数目 + 每列车：车组号/表号/车次号/目的地码/集中站站号/车站编号/上下行侧车站/激活端/运行方向/出入库/转换轨/折返轨/站台/失去位置/逻辑物理区段/区间次序/编组/驾驶员/停车超时/早晚点/驾驶模式/车门/停车/控制级别/无人折返/紧急制动/满载率/速度/停车过标欠标/雨雪火灾脱轨停站超时/前方进路 |
| 0x07 | ISCS→SIG | 供电状态消息 | 供电区间数目 + 各区段编号/状态（0未知/1已供电/2未供电/其他按枚举） |
| 0x08 | ISCS→SIG | 火灾状态消息 | 火灾分区数目 + 各分区编号/状态（1激活/2未激活/其他按枚举） |
| 0x0E | SIG→ISCS | 越站命令信息 | 车站编号 + 站台编号 + 车组号/表号/车次号 + 预留 |
| 0x10 | SIG→ISCS | 设备状态全体消息 | 集中站号 + 类型数量 + 每设备类型：设备类型/设备数目 + 每设备：设备唯一识别号/状态位（信号机/道岔/联锁按位解码） |
| 0x11 | ISCS→SIG | 外部报警信息 | 报警类型（客流/火灾按枚举）+ 车站编号 + 报警级别 + 参数1/参数2 |
| 0x13 | ISCS→SIG | 区间水位报警消息 | 报警数目 + 各报警点编号/状态 |

方向说明：`SIG→ISCS` 即 ATS→综合监控；`ISCS→SIG` 即综合监控→ATS。消息定义中 A=ATS→ISCS、I=ISCS→ATS、B=双向，实际方向结合 TCP 端口方向判定。

### 2. 信息帧与消息结构

信息帧（帧偏移 0 起算）：

```
EF EF | Frame_Count(1) | Frame_Index(1) | Data_Len(2) | Data(n≤1024) | FD FD
```

- `Frame_Count`：总帧数；`Frame_Index`：当前帧序号，从 1 开始
- 数据长度 > 1024 字节时拆分为多帧发送；多个消息也可合并进一帧

消息（相对帧头偏移 6 起算）：

```
Message_Len(2) | Message_Time(7) | Line_Id(2) | Validity(1) | Spare(17) | Message_Id(1) | version(1) | Content
```

- `Message_Len`：消息长度，不含长度本身，≤65533
- `Message_Time`：7 字节，年(2LE)+月/日/时/分/秒各 1，如 2001-09-21 15:29:30 → `D1-07-09-15-0F-1D-1E`；无效时间默认为 1970-01-01 00:00:00
- 全部多字节数值低字节在前（LE），字符串 ANSI

### 3. 跨帧消息重组

- 消息跨多个信息帧（`Frame_Count>1`）时，按 `Frame_Index` 缓存各帧 Data 片断
- 全部帧到齐后用 `ByteArray` 直接合并（避免 string→ByteArray 遇 NUL 截断），再整体解析
- 重组消息统一在最后一帧的树中显示 `重组消息(跨N帧)`，字段字节序按**消息起始所在帧**的帧头起算
- 支持帧片断乱序到达（不依赖到达顺序，按 Frame_Index 归位）

### 4. 帧内多消息与 TCP desegment

- 一帧内可含多条完整消息，逐一解析
- TCP 段不完整时通过 `pinfo.desegment_len/offset` 请求重组，等待完整帧再解析
- 一个 TCP 段可能含多帧，递归处理剩余数据

### 5. 字段标注与枚举中文

- 每个字段标注：名称、协议字段名 `[filter]`、字节长度 `(Nbytes)`、字节序 `(N)`、值
  - 例：`车组号[Train_id](1+N),字节序(43):001`
- 全部字段已注册 `dgl1.*` ProtoField，可在显示过滤器按字段过滤
- 枚举字段自动追加 ` | 中文`，如 `2 | 定位`、`1 | 有效`

### 6. 数据健壮性

- 帧头/帧尾校验（`0xEF 0xEF` / `0xFD 0xFD`），非法帧交回 data dissector
- 消息长度域合理性校验（1~65533），不完整/非法时给出 ⚠ 告警并跳出
- `Data_Len` 上限 1024 校验，越界视为非本协议

---

## 二、使用方法

### 1. 安装

- 文件 `dgl1_protocol.lua` 存放于 Wireshark 个人插件目录（Windows）：
  `%APPDATA%\Wireshark\plugins\`
- 启动 Wireshark 自动加载；运行中修改后按 **Ctrl+Shift+L** 重载 Lua 插件
- 插件必须位于 `plugins\` 根目录（Wireshark 不递归扫描子目录）

### 2. 端口配置

- 默认监听端口：`2100`（ATS 服务端实时端口）
- 通过 **分析（Analyze）→ 协议首选项（Protocols）→ DGL1 Protocol** 修改
- 支持多端口，用空格分隔，如 `2100 2101`
- 修改后自动重新绑定端口

### 3. 报文显示

协议树字段格式：

```
DGL1 信息帧, Data_len:105 (帧 1/1)
    帧头[Frame_Head](2bytes),字节序(0):0xEF 0xEF
    总帧数[Frame_Count](1bytes),字节序(2):1
    当前帧序号[Frame_Index](1bytes),字节序(3):1
    Data域长度[Data_Len](2bytes),字节序(4):105
    Data(消息):
        Message Head
            消息长度[Message_Len](2bytes),字节序(6):103
            消息发送时间[Message_Time](7bytes),字节序(8):2024-09-21 15:29:30
            线路号[Line_Id](2bytes),字节序(15):144
            消息有效标识[Validity](1bytes),字节序(17):0 | 有效
            消息ID[Message_Id](1bytes),字节序(35):0x05 列车位置信息
            版本号[version](1bytes),字节序(36):0x01
        Content:列车位置信息 ,len:72
            列车数目[Trains_cnt](1bytes),字节序(37):1
            车:1
                车组号[Train_id](1+N),字节序(38):001
                ...
```

### 4. 显示过滤器

全部字段已注册 `dgl1.*` 过滤器，常用示例：

| 过滤器 | 含义 |
|--------|------|
| `dgl1` | 所有 DGL1 报文 |
| `dgl1.msg_id == 0x05` | 列车位置信息 |
| `dgl1.line_id == 144` | 线路号 144 |
| `dgl1.train_id == "001"` | 指定列车车组号 |
| `dgl1.station_id == 10` | 指定车站编号 |
| `dgl1.rtu_id == 1` | 指定集中站站号 |
| `dgl1.dev_state == 0x00000001` | 设备状态（hex） |
| `dgl1.frame_count > 1` | 跨帧消息相关帧 |

### 5. Info 列

- 显示 `[方向] 消息名 line=线路号`，如 `[SIG→ISCS] 列车位置信息 line=144`
- 方向由 TCP 端口方向 + 消息定义判定（双向消息按端口方向显示）
- 消息有效标识为无效时追加 ` 无效`

### 6. 常见问题

- **跨帧消息没解析**：确认在 Wireshark 里按 **Ctrl+Shift+L** 重载过插件（运行中改代码后必须重载）；跨帧消息在最后一帧的树中显示
- **帧 11 显示原始 TCP**：跨帧片断帧的 Data 不足一个完整消息头，属正常，完整消息在末帧解析
- **未识别协议**：确认端口已配置；若报文非本协议，插件自动交回 data dissector
- **命令行验证**：`tshark -r test.pcap -Y "dgl1" -V`（tshark 与 Wireshark 共用插件目录和 Lua 引擎）

---

## 三、变更记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| 1.0.3 | 2026-08-10 | 依据 dgl1_ats.md V1.1 实现全部消息类型；信息帧结构 + 帧内多消息 + 跨帧重组（Frame_Index 拼接）+ TCP desegment；全部字段可过滤并标注字节序；枚举字段追加中文描述；线路号入 Info 列 |

---

## 四、环境

- Wireshark 4.6.7（内置 Lua 5.4）
- 文件名：`dgl1_protocol.lua`
- 依赖：仅 Wireshark 内置 Lua 引擎，无第三方库

---

## 五、附录：本插件要点

- **字节序**：信息帧/消息内所有多字节数值均为低字节在前（LE），字段标注的 `字节序(N)` 是字段相对帧头的绝对偏移，便于与抓包字节对位
- **跨帧重组**：不依赖到达顺序，按 `Frame_Index` 归位，全部到齐后合并解析；重组消息字段偏移按起始帧起算
- **方向判定**：`2100` 端口为 SIG（ATS 服务端）；源端口命中即 SIG 发起，否则按目的端口
- **通用 Wireshark Lua 开发指南**（插件目录/重载/调试/常用 API/常见坑）：见 `cql15_protocol_VERSION.md` 附录
