# FSL2 TCMS Protocol 插件版本说明

## 版本 1.0.0 (2026-08-12)

### 概述

佛山地铁2号线 ISCS-PIS 接口 Wireshark Lua 解析插件，依据《FSL02-JK-10028-ISCS-PIS-TCMS协议文件》V01 实现综合监控系统 ISCS 与地面 PIS 系统（车载 TCMS 状态数据经 PIS 透传）之间通信报文的解析。

本插件将 ISCS-PIS 接口的 UDP 二进制应用层报文解析为结构化的字段树。报文封装在数据帧中（帧头 0x7F / 消息序号 / 设备序号 / 消息类型 / 起始地址 / 消息内容 / 包校验 / 帧尾 0x7E），支持帧内多消息、数据点表逐字节解析与截断/错位报文告警。

---

## 一、实现的功能

### 1. 消息类型解析（4 种）

| 消息类型 | 方向 | 消息 | 总长 | 解析内容 |
|----------|------|------|------|----------|
| 0x01 | ISCS→PIS | 会话建立信令 | 12 字节 | 消息内容 4 字节 = ISCS 前置机 IP |
| 0x02 | PIS→ISCS | 心跳信令 | 8 字节 | 无数据域 |
| 0x04 | 双向 | 确认报文 | 9 字节 | 确认内容 1 字节（0x01 确认收到） |
| 0x10 | PIS→ISCS | 数据信令 | 38 字节 | 30 字节点表数据，按起始地址逐字节显示 |

- 0x04 方向按设备序号判定：设备 0xF0（ISCS）发起为 ISCS→PIS，0xF5（PIS）发起为 PIS→ISCS
- 消息类型不在上表取值时按 unknown 整段显示，不误判为其他协议

### 2. 数据帧格式

```
0x7F | 消息序号(1) | 设备序号(1) | 消息类型(1) | 起始地址(2) | 消息内容(N) | 包校验(1) | 0x7E
```

- 帧头固定 0x7F、帧尾固定 0x7E；多字节数值按网络字节序（大端）
- 设备序号：列车 1~25 / ISCS 0xF0 / PIS 0xF5
- 包校验：从消息序号到消息内容结束求和，取低字节（与规范 3.2.3 示例互证）

### 3. 数据点表解析（0x10 数据信令）

- 起始地址仅对数据信令有效，取值为 30 的整数倍；非 30 整数倍时追加 `[WARN: 起始地址不是30整数倍]` 告警
- 消息内容 30 字节逐字节解析为 `fsl2tcms.val`，每个字节标注相对帧头的字节序与点表绝对地址
- 显示树中提示点表地址范围 `数据点表: 地址 start ~ start+29`

### 4. 包校验与数据健壮性

- 包校验按规范算法重算，显示 `[OK]` / `[FAIL(计算值=0xXX)]`
- 帧尾校验：非 0x7E 时提示 `异常(应为0x7E)`
- 报文不足 6 字节或帧头非 0x7F：交回 Wireshark 自带 data dissector，不误判
- 一个 UDP 数据报可含多条消息，逐一解析；多消息时 Info 列以最后一条为准
- 消息被截断时显示已有字节并标注 `[截断]`；帧边界错位（中间帧头非 0x7F）时余下按原始数据显示，不丢弃

---

## 二、使用方法

### 1. 安装

- 文件 `fsl2_tcms_protocol.lua` 存放于 Wireshark 个人插件目录（Windows）：
  `%APPDATA%\Wireshark\plugins\`
- 启动 Wireshark 自动加载；运行中修改后按 **Ctrl+Shift+L** 重载 Lua 插件
- 插件必须位于 `plugins\` 根目录（Wireshark 不递归扫描子目录）

### 2. 端口配置

- 默认监听端口：`18010,18030`（ISCS 收 18010 / PIS 收 18030）
- 通过 **分析（Analyze）→ 协议首选项（Protocols）→ FSL2 TCMS Protocol** 修改
- 支持多端口，用逗号分隔，如 `18010,18030`
- 修改后自动重新绑定端口

### 3. 报文显示

协议树字段格式（名称 + 过滤器名 + 字节序）：

```
FSL2 TCMS Data (12 bytes) | 会话建立信令 ISCS→PIS
    数据帧头(head)(1bytes),字节序(0):0x7f
    消息序号(seq)(1bytes),字节序(1):1
    设备序号(device)(1bytes),字节序(2):0xf0 (ISCS)
    消息类型(message)(1bytes),字节序(3):0x01
    起始地址(startaddr)(2bytes),字节序(4):0
    ISCS IP(ip)(4bytes),字节序(6):192.168.0.1
    包校验(crc)(1bytes),字节序(10):0x5b [OK]
    数据帧尾(tail)(1bytes),字节序(11):0x7e [OK]
```

- 每个字段标注：名称、协议字段名 `[filter]`、字节长度 `(Nbytes)`、字节序 `(N)`、值
- 设备序号自动追加名称：`0xf0 (ISCS)`、`0xf5 (PIS)`、`0x01 (列车1)`
- 校验/告警类字段自动追加状态：`[OK]` / `[FAIL]` / `[WARN]`

### 4. 显示过滤器

全部字段已注册 `fsl2tcms.*` 过滤器，常用示例：

| 过滤器 | 含义 |
|--------|------|
| `fsl2tcms` | 所有 FSL2 TCMS 报文 |
| `fsl2tcms.device == 0xf0` | 设备序号 0xF0（ISCS） |
| `fsl2tcms.messageid == 0x10` | 数据信令 |
| `fsl2tcms.startaddr == 90` | 点表起始地址 90 |
| `fsl2tcms.val == 1` | 点表值为 1 的字节 |
| `fsl2tcms.ip == 192.168.0.1` | 会话信令中的 ISCS IP |

### 5. Info 列

- 显示 `消息类型 方向 seq=序号 [有效信息]`，如 `数据信令 PIS→ISCS seq=5 列车1 addr=90`、`会话建立信令 ISCS→PIS seq=1 IP=192.168.0.1`
- 未知类型显示 `未知类型(0xXX) seq=序号`；多消息同报时以最后一条为准；截断消息追加 `[截断]`

### 6. 常见问题

- **未识别协议**：确认端口已配置；若报文非本协议，插件自动交回 data dissector
- **数据信令显示 WARN**：起始地址不是 30 整数倍，对照《佛山地铁2号线 车辆数据下地点表》确认地址
- **命令行验证**：`tshark -r test\fsl2_test.pcap -Y "fsl2tcms" -V`（tshark 与 Wireshark 共用插件目录和 Lua 引擎）

---

## 三、变更记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| 1.0.0 | 2026-08-12 | 依据 FSL02-JK-10028 V01 实现 ISCS-PIS-TCMS 协议解析；4 种消息类型（会话建立/心跳/确认/数据信令）；数据点表逐字节解析并标注点表地址；包校验与帧尾校验；一报多消息/截断/错位健壮性处理；全部字段可过滤并标注字节序；设备序号追加中文名称 |

---

## 四、环境

- Wireshark 4.6.7（内置 Lua 5.4）
- 文件名：`fsl2_tcms_protocol.lua`
- 依赖：仅 Wireshark 内置 Lua 引擎，无第三方库

---

## 五、附录：本插件要点

- **字节序**：多字节数值按网络字节序（大端），字段标注的 `字节序(N)` 是字段相对帧头的绝对偏移，便于与抓包字节对位
- **方向判定**：按设备序号 + 消息类型判定（0x01 恒为 ISCS→PIS；0x04 按设备号区分；0x02/0x10 恒为 PIS→ISCS）
- **端口**：注册的是 ISCS 收（18010）与 PIS 收（18030）两个接收端口，消息方向由报文内容判定而非端口
- **通用 Wireshark Lua 开发指南**（插件目录/重载/调试/常用 API/常见坑）：见 `cql15_protocol_VERSION.md` 附录
