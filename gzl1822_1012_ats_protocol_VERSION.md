# GZL1822/1012 ATS Protocol 插件版本说明

## 版本 1.0.0 (2026-08-13)

### 概述

广州轨道交通 18&22 号线 / 10 号线 ATS-ISCS 接口 Wireshark Lua 解析插件，依据《GZL18&22-S1-1903》V1.1（`gzl18_ats.md`）与《GZL10-S1-1903》V1.9（`gzl10_ats.md`）实现信号 ATS 系统与综合监控 ISCS 系统之间全部 TCP 传输消息类型的解析。

本插件将 ATS-ISCS 接口的二进制应用层报文解析为结构化的字段树。报文封装在**信息帧**中（帧头 `0xEF 0xEF` / 总帧数 / 当前帧 / 长度 / 设备主备状态 / 帧尾 `0xFD 0xFD`），应用帧为 `消息类型(2B LE) + 内容`，支持跨帧消息重组与 TCP desegment。

---

## 一、实现的功能

### 1. 消息类型解析（13 种）

| Type | 方向 | 消息 | 解析内容 |
|------|------|------|----------|
| 0x01 | 双向 | 心跳信息 | 状态(4) = 0xFFFFFFFF |
| 0x02 | ATS→ISCS | 列车信息 | 列车个数 + 每列车：车组号/车次号/方向/集中站编号/前端分区ID/前端偏移量/紧急制动/阻塞/休眠唤醒/工况/授权确认/发车请求/CAM请求/清客站台ID/欠超标/跳跃/火灾报警；GZL10 追加 驾驶模式/停车精度/速度/预留 |
| 0x03 | ATS→ISCS | 计划信息 | 站台数 + 每站台：车站编号/站台编号/列车趟数 + 每趟：车组号/车次号/停车类型/计划到离站时间/终到站/当前状态/扣车/大站快车/下一停车站/清客 |
| 0x04 | ATS→ISCS | 首末班信息 | 站台数 + 每站台：首班/末班 车组号/车次号/停车类型/计划到离站时间/终到站 |
| 0x05 | ATS→ISCS | 车门隔离状态信息 | 站台ID + 两侧站台门ID/故障隔离要求（8字节位图，大端） |
| 0x06 | ATS→ISCS | 站台门隔离状态信息 | 站台个数 + 每站台：两侧站台门ID/隔离要求/打开状态（大端） |
| 0x07 | ATS→ISCS | 站场表示信息 | RTU状态 + 车站个数 + 每车站：站码/信息长 + 每设备：设备类型/编号/色码（按附录4位解码） |
| 0x08 | ISCS→ATS | 供电分区信息 | 供电区段数目 + 各区段编号/状态（0未知/1供电中/2未供电） |
| 0x09 | ISCS→ATS | 首末班申请 | 申请(4) = 0x00000000 |
| 0x10 | ATS→ISCS | 场段出入段计划信息（GZL10） | 调度日/图号 + 每服务组：出入段车次号/车组号/股道/转换轨/正线到达/目的地码/初始化/洗车/唤醒/清扫/休眠 |
| 0x11 | ISCS→ATS | 车站火灾信息（GZL10） | 车站数目 + 各车站编号/火灾状态（0未知/1有火灾/2无火灾） |
| 0x12 | ISCS→ATS | 区间火灾信息（GZL10） | 区间数目 + 各区间编号/火灾状态（0无/1有） |
| 0x13 | ISCS→ATS | 区间轨面水位告警信息（GZL10） | 传感器数目 + 各传感器编号/告警标志 |

方向说明：`ATS→ISCS` 即 ATS 服务端发起，`ISCS→ATS` 即综合监控发起，按 TCP 端口方向判定（ATS 为服务端、监听 10020）。

### 2. 信息帧与应用帧结构

信息帧（帧偏移 0 起算）：

```
EF EF | Total(1) | Index(1) | Data_Len(2 LE) | Dev_Status(1) | Data(n≤2048) | FD FD
```

- `Total`：总帧数（1~32）；`Index`：当前帧序号（1~32），长度超限的应用数据分包发送，`Index==Total` 时合并处理
- `Data_Len`：Data 域长度，低字节在前；`Dev_Status`：0x01 主机 / 0x02 备机
- 帧尾固定 `0xFD 0xFD`，帧头固定 `0xEF 0xEF`

应用帧（即 Data 域）：

```
Type(2 LE) | Content((n-2) b)
```

- `Type`：消息种类，低字节在前；`Content`：具体消息内容，因消息种类而异

### 3. 跨帧消息重组

- 应用数据跨多帧（`Total>1`）时，按 `Index` 缓存各帧 Data 片断
- 全部帧到齐后用 `ByteArray` 直接合并（避免 string→ByteArray 遇 NUL 截断），再整体解析
- 重组消息统一在最后一帧的树中显示 `重组应用帧(跨N帧)`；片断乱序到达不影响（按 Index 归位）

### 4. 列车信息 0x02 的 GZL18/GZL10 自动识别

- 18&22 号线与 10 号线共用端口 10020，无法按端口区分线路
- 两线列车记录在公共字段之后尾部字节数不同：**GZL18 无尾（0 字节）**，**GZL10 追加 15 字节**（驾驶模式 1 + 停车精度 2 + 速度 2 + 预留 10）
- 插件按「公共字段后残差 = 0 / 15」自动识别并标注 `识别线路: GZL18/GZL10`，支持多列车

### 5. 设备色码位解码（0x07，附录4）

按设备类型解码色码位：

| 设备类型 | 解码内容 |
|----------|----------|
| 0x11 逻辑区段 | 非通信占用/锁闭/故障锁闭/封锁/CBTC占用/保护区段锁闭/区段切除/计轴复位/ARB故障 |
| 0x14 道岔 | 位置（四开/定位/反位/挤岔）+ 占用/锁闭/单锁/单封/切除/封锁/ARB故障 |
| 0x21 信号机 | 低位/高位颜色（无色~青）+ 闪烁 + 室外关闭 |
| 0x52 ESB按钮 | 站台紧急关闭 |
| 0x62 防淹门 | 门关开/关门请求/关门允许 |
| 0x63 SPKS状态灯 | 灯灭/灯亮 |
| 0x64 SPKS旁路按钮 | 按钮按下 |

### 6. 字段标注与数据健壮性

- 每个字段标注：名称、协议字段名 `[filter]`、字节长度 `(Nbytes)`、字节序（相对 Data 域起算）`(N)`、值；字节以十六进制显示，避免 NUL 截断标签
- 全部字段已注册 `gzlats.*` ProtoField，可在显示过滤器按字段过滤
- 枚举字段自动追加 ` | 中文`，如 `0x55 | 紧急制动`、`2 | 供电中`
- 帧头/帧尾校验，非法帧交回 data dissector；`Data_Len` 上限 2048 校验
- 数据不完整/结构长度不符时给出 ⚠ 告警并跳出，不误判
- 一个 TCP 段可能含多帧，递归处理剩余数据；TCP 段不完整时通过 `pinfo.desegment_len/offset` 请求重组

---

## 二、使用方法

### 1. 安装

- 文件 `gzl1822_1012_ats_protocol.lua` 存放于 Wireshark 个人插件目录（Windows）：
  `%APPDATA%\Wireshark\plugins\`
- 启动 Wireshark 自动加载；运行中修改后按 **Ctrl+Shift+L** 重载 Lua 插件
- 插件必须位于 `plugins\` 根目录（Wireshark 不递归扫描子目录）

### 2. 端口配置

- 默认监听端口：`10020`（ATS 服务端）
- 通过 **分析（Analyze）→ 协议首选项（Protocols）→ GZL1822/1012 ATS Protocol** 修改
- 支持多端口，用逗号分隔，如 `10020,10021`
- 修改后自动重新绑定端口

### 3. 报文显示

协议树字段格式：

```
GZL ATS 信息帧, Data_len:71 (帧 1/1)
    帧头[frame_head](2bytes),字节序(0):0xEF 0xEF
    总帧数[frame_total](1bytes),字节序(2):1
    当前帧序号[frame_index](1bytes),字节序(3):1
    Data域长度[data_len](2bytes),字节序(4):71
    设备主备状态[dev_status](1bytes),字节序(6):0x01 | 主机
    Data(应用帧): 列车信息 (Type=0x02)
        消息类型[msg_type](2bytes),字节序(0):0x0002 列车信息
        Content:列车信息 ,len:69
            列车个数[train_cnt](2bytes),字节序(0):2
            识别线路: GZL18 (每列车尾0字节)
            车:1
                车组号[group_no](4bytes),字节序(3):T001
                ...
    帧尾[frame_tail](2bytes),字节序(78):0xFD 0xFD
```

### 4. 显示过滤器

常用示例：

| 过滤器 | 含义 |
|--------|------|
| `gzlats` | 所有 GZL ATS 报文 |
| `gzlats.msg_type == 0x02` | 列车信息 |
| `gzlats.dev_status == 0x02` | 备机发送的帧 |
| `gzlats.train_cnt > 0` | 含列车信息的帧 |
| `gzlats.drive_mode == 0x05` | GZL10 驾驶模式 FAM |
| `gzlats.dev_type == 0x11` | 逻辑区段设备 |
| `gzlats.section_state == 1` | 供电中区段 |
| `gzlats.frame_total > 1` | 跨帧消息相关帧 |

### 5. Info 列

- 显示 `[方向] 消息名 type=0xXX [个数]`，如 `[ATS→ISCS] 列车信息 type=0x02 列车数=2`、`[ISCS→ATS] 供电分区信息 type=0x08 区段数=2`
- 跨帧片断帧不设 Info，完整消息在末帧解析；未知类型显示 `未知类型(0xXX)`

### 6. 常见问题

- **未识别协议**：确认端口已配置；若报文非本协议，插件自动交回 data dissector
- **0x02 识别线路异常**：列车公共字段后残差非 0/15 时给出 ⚠ 告警，对照抓包确认（两线消息混连时会误判，建议分线抓包）
- **跨帧消息没解析**：确认运行中改代码后已 **Ctrl+Shift+L** 重载；跨帧消息在最后一帧的树中显示
- **命令行验证**：`tshark -r test\gzl_all.pcap -Y "gzlats" -V`（tshark 与 Wireshark 共用插件目录和 Lua 引擎）

---

## 三、变更记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| 1.0.0 | 2026-08-13 | 依据 gzl18_ats.md V1.1 与 gzl10_ats.md V1.9 实现全部 13 种 TCP 消息类型；信息帧结构 + 跨帧重组（Index 拼接）+ TCP desegment；0x02 列车信息 GZL18/GZL10 自动识别；0x05/0x06 按大端解析；0x07 设备色码按附录4 位解码；全部字段可过滤并标注字节序；测试：test/gen_gzl_all.py + test/gzl_all.pcap（20 包覆盖全部类型） |

---

## 四、环境

- Wireshark 4.6.7（内置 Lua 5.4，含位运算扩展）
- 文件名：`gzl1822_1012_ats_protocol.lua`
- 依赖：仅 Wireshark 内置 Lua 引擎，无第三方库

---

## 五、附录：本插件要点与待确认项

- **字节序**：默认多字节低字节在前（LE）；0x05/0x06 车门/站台门隔离按规范注明为高字节在前（BE）
- **方向判定**：`10020` 为 ATS 服务端端口；源端口命中即 ATS 发起，否则按目的端口
- **跨帧重组**：不依赖到达顺序，按 `Index` 归位，全部到齐后合并解析
- **字段字节序标注**：统一相对 Data 域（应用帧）起算，跨帧重组消息同此约定
- **待实测确认项**：
  1. 0x07「站场表示信息长」按设备段长度解释（不含站码+信息长 6 字节头）；若实抓为整站长度需调整
  2. 0x09 首末班申请在 gzl18 清单来源标注 ATS、发送条件为 ISCS（规范自相矛盾）；插件按 TCP 端口方向显示，不依赖消息定义来源
- **通用 Wireshark Lua 开发指南**（插件目录/重载/调试/常用 API/常见坑）：见 `cql15_protocol_VERSION.md` 附录
