# JNL7 Protocol 插件版本说明

## 版本 1.0.0 (2026-08-10)

### 概述

济南7号线 ATS-ISCS 接口 Wireshark Lua 解析插件，依据《济南城市轨道交通7号线一期工程ATS与综合监控系统接口规格书》V1.0（`jnl7_ats.md`）实现信号 ATS 系统与综合监控 ISCS 系统之间全部消息类型的解析。

本插件将 SIG-ISCS 接口的二进制应用层报文解析为结构化的字段树，支持按字段过滤、枚举中文解读、CRC 累加和校验与 TCP 分包重组。

---

## 一、实现的功能

### 1. 消息类型解析（11 种）

| 类型 | 方向 | 消息 | 解析内容 |
|------|------|------|----------|
| 01 | SIG→ISCS | 列车运行信息及列车阻塞信息 | 每列车：车组号/司机号/CBTC状态/位置/运行方向/车站/轨道/阻塞/区段名称/早晚点/扣车/立即停车/清客/跳停/休眠/唤醒/工况/湿轨模式 |
| 02 | SIG→ISCS | 站台信息 | 车站/站台号/紧停/列车数 + 每列车：车组/编组/表号/车次/首末班/站台状态/到离站时间/终点/目的地码/载客/接近/轧道/快慢 |
| 06 | SIG→ISCS | 当天使用的时刻表 | 总包数/当前包/表号/行程数 + 每行程：车次/目的地码/站台数量 + 每站台：车站/站台号/到离站时间 |
| 07 | ISCS→SIG | SCADA 供电区段 | 区段数量 + 各区段状态（0未知/1已供电/2未供电） |
| 08 | 双向 | 回执信息 | 头部 + CRC（无数据域，固定 31 字节） |
| 09 | 双向 | 心跳信息 | 头部 + CRC（无数据域，固定 31 字节） |
| 15 | ISCS→SIG | 客流信息 | 客流个数 + 每客流 8 字节 |
| 17 | ISCS→SIG | 站台运营载客首末班车信息 | 首/末班载客列车发车时间、终点站号 |
| 19 | SIG→ISCS | 段场出入库信息 | 车组/位置/当前运行轨道/库线股道/出入库状态 |
| 19 | ISCS→SIG | FAS 火灾信息 | FAS 数量 + 各 FAS 状态（1激活/2未激活） |
| 21 | ISCS→SIG | 区间超水位信息 | 区间数量 + 各区间状态 |
| 39 | SIG→ISCS | 信号设备状态信息 | 设备数量 + 每设备：信号类型/设备名称 + 2 字节状态位按信号类型解码 |

### 2. 类型 19 方向自动区分

`信息类型` 19 在规格书中既是「段场出入库」（SIG 发起）又是「FAS 火灾信息」（ISCS 发起）。插件按 **TCP 端口方向** 自动区分：

- 报文的源端口是注册端口 → SIG 发起 → 解析为 **段场出入库**
- 报文的目的端口是注册端口 → ISCS 发起 → 解析为 **FAS 火灾信息**

Info 列同时标注方向 `[SIG→ISCS]` / `[ISCS→SIG]`。

### 3. CRC 校验

- 依据附录1累加和算法：对报文所有字节求和取低字节，以两位大写十六进制 ASCII 表示
- 每包末尾显示 `CRC校验[crc]:XX [OK]` 或 `[FAIL(calc=YY)]`
- CRC 定位以**结构解析结果**为准（而非长度域），避免无数据域消息长度域不可靠导致定位错

### 4. 信号设备状态解码（类型 39）

- 2 字节设备状态按信号类型（信号机/道岔）逐位解码：
  - **信号机**：高位字节作色码（0绿灯/1黄灯/2红灯/3引导）+ 低位字节位标志（CBTC/闪灯/封锁/Fleet进路/绿黄灯丝断丝/红灯断丝/蓝灯断丝）
  - **道岔**：低位字节位标志（失表/定位/反位等）
- 高位字节与低位字节分别标注，状态含义自动追加中文

### 5. TCP 分包重组（desegment）

- 单条消息跨多个 TCP 段时自动重组
- 单 TCP 段含多条消息时逐一解析
- 同段多包时循环内先校验长度域完整性，避免越界

### 6. 数据健壮性

- 长度域合理性校验（21~32767），非法数据交回 data dissector
- 结构长度与长度域不一致时给出 ⚠ 告警
- 头部长度不足时等待重组而非崩溃

---

## 二、使用方法

### 1. 安装

- 文件 `jnl7_protocol.lua` 存放于 Wireshark 个人插件目录（Windows）：
  `%APPDATA%\Wireshark\plugins\`
- 启动 Wireshark 自动加载；运行中修改后按 **Ctrl+Shift+L** 重载 Lua 插件
- 插件必须位于 `plugins\` 根目录（Wireshark 不递归扫描子目录）

### 2. 端口配置

- 默认监听端口：`5000`
- 通过 **分析（Analyze）→ 协议首选项（Protocols）→ JNL7 Protocol** 修改
- 支持多端口，用空格分隔，如 `5000 10020 10030`
- 修改后自动重新绑定端口

### 3. 报文显示

协议树字段格式（编号.名称 + 字节序 + 过滤器名）：

```
JNL7 Protocol Data, Len: 56
    HEAD
        1.信息头标识(1bytes),字节序(0):0x01
        2.信息序号[seq](2bytes),字节序(1):03
        3.信息类型[data_type](2bytes),字节序(3): 19
        4.信息长度[data_len](5bytes),字节序(5):00046
        5.数据发送时间[send_time](19bytes),字节序(10):2009-21-05 09:10:34
    Data:段场出入库信息 ,type:19 ,len:27
        6.列车位置[train_pos](1bytes),字节序(34):2 | 车辆段
        ...
    CRC校验[crc](2bytes),字节序(54):... [OK]
```

- 每个字段标注：编号、名称、字节长度 `(Nbytes)`、字节序 `(X)`、过滤器名 `[field]`、值
- 枚举字段自动追加 ` | 中文`，如 `2 | 车辆段`、`1 | 出库`、`0 | 未激活`
- 循环体（多列车/多设备/多行程）的字节序自动累加

### 4. 显示过滤器

全部字段已注册 `jnl7.*` 过滤器，常用示例：

| 过滤器 | 含义 |
|--------|------|
| `jnl7` | 所有 JNL7 报文 |
| `jnl7.data_type == "19"` | 段场出入库 / FAS（按方向区分） |
| `jnl7.train_set == "15271001"` | 指定列车车组号 |
| `jnl7.station_no == "1502"` | 指定车站编号 |
| `jnl7.eb_state == "1"` | EB 激活 |
| `jnl7.drive_mode == "4"` | AM 模式 |
| `jnl7.dev_state == 0x0001` | 信号设备状态（hex） |

### 5. Info 列

- 显示 `[方向] 消息名 seq=序号`，如 `JNL7 [SIG→ISCS] 段场出入库信息 seq=03`
- 方向由 TCP 端口方向判定（类型 19 据此区分出入库/FAS）

### 6. 常见问题

- **Trailing stray characters**：已通过 `send_time`/`arrive_time`/`depart_time`/`first_time`/`last_time` 改 FT_BYTES 消除（时间域含 NUL 分隔符，FT_STRING 会截断触发告警）
- **未识别协议**：确认端口已配置；若报文非本协议，插件自动交回 data
- **CRC 显示 FAIL**：抓包校验和与重算值不符时给出告警，可结合报文是否为中途截断判断

---

## 三、变更记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| 1.0.0 | 2026-08-10 | 依据 jnl7_ats.md V1.0 实现全部消息类型；类型19按方向区分段场出入库/FAS；CRC 累加和校验；类型39信号设备状态按位解码；全部字段可过滤并标注字节序；时间域 FT_BYTES 消除 stray |

---

## 四、环境

- Wireshark 4.6.7（内置 Lua 5.4）
- 文件名：`jnl7_protocol.lua`
- 依赖：仅 Wireshark 内置 Lua 引擎，无第三方库

---

## 五、附录：本插件要点

- **CRC 定位**：以结构解析结果定位 CRC 字段，而非长度域，避免无数据域消息（08/09）长度域不可靠导致定位错
- **类型 19 冲突**：出入库（SIG）与 FAS（ISCS）共用类型号，靠源/目的端口方向区分
- **FT_BYTES**：所有含 NUL 分隔符的时间域字段用 `ProtoField.bytes`，避免 FT_STRING 截断触发 Trailing stray characters
- **方向判定**：`5000` 端口为 SIG（ATS 服务端）；源端口命中即 SIG 发起，否则按目的端口
- **通用 Wireshark Lua 开发指南**（插件目录/重载/调试/常用 API/常见坑）：见 `cql15_protocol_VERSION.md` 附录
