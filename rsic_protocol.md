# RSIC Protocol Wireshark 插件

## 插件简介

RSIC Protocol 是一个用于解析广州地铁信号系统（SIG）协议的 Wireshark Lua 插件。该插件支持广州地铁多条线路的协议解析，包括：

- **广州18号线 / 11号线 / 22号线**
- **广州10号线 / 12号线**

### 基本信息

- **插件名称**: RSIC Protocol
- **版本**: 1.0.1#20250130-1
- **作者**: yearing
- **协议类型**: TCP 应用层协议
- **默认端口**: 10020, 10030, 10090
- **最后更新**: 2025-01-30

## 功能特性

### 1. 支持的数据类型

| 数据类型 | 类型值 | 说明 |
|---------|--------|------|
| unknown | 0 | 未知类型 |
| heartbeat | 1 | 心跳信息 |
| train | 2 | 列车信息 |
| plan | 3 | 计划信息 |
| firstLastTrain | 4 | 首末班信息 |
| trainDoor | 5 | 车门隔离状态信息 |
| platformDoor | 6 | 站台门隔离状态信息 |
| stationYard | 7 | 站场表示信息 |
| power | 8 | 供电臂信息 |
| firstLastTrainRequest | 9 | 首末班申请 |
| stationPlan | 16 | 场段出入段计划信息 |
| stationFire | 17 | 车站火灾信息 |
| zoneFire | 18 | 区间火灾信息 |
| zoneWater | 19 | 区间水灾信息 |

### 2. 核心功能

- ✅ **分包自动重组**: 支持TCP分包自动拼接和重组
- ✅ **多线路支持**: 根据配置自动切换不同线路的解析逻辑
- ✅ **详细字段解析**: 完整解析列车信息、计划信息、供电信息等
- ✅ **状态值映射**: 将数值状态转换为可读中文文本
- ✅ **时间格式化**: 自动将时间戳转换为标准格式（YYYY-MM-DD HH:MM:SS）
- ✅ **错误检测**: 自动检测并标记协议错误
- ✅ **中文界面**: 所有字段和状态值均提供中文显示
- ✅ **会话隔离**: 每个TCP连接独立缓存，避免数据串扰

### 3. 线路差异

#### 广州18/11/22号线
- 列车数据长度: 26字节（基础）
- 支持功能: 心跳、列车信息、计划信息、首末班、车门、供电臂

#### 广州10/12号线
- 列车数据长度: 41字节（基础）
- 支持功能: 包含18号线所有功能 + 站场信息、场段计划、火灾/水灾信息

## 安装方法

### Windows 系统

1. 找到 Wireshark 插件目录：
   - 用户插件目录: `%APPDATA%\Wireshark\plugins\`
   - 系统插件目录: `C:\Program Files\Wireshark\plugins\`

2. 将 `rsic_protocol.lua` 文件复制到插件目录

3. 重启 Wireshark

### Linux/macOS 系统

1. 找到 Wireshark 插件目录：
   - 用户插件目录: `~/.local/lib/wireshark/plugins/`
   - 系统插件目录: `/usr/lib/wireshark/plugins/`

2. 将 `rsic_protocol.lua` 文件复制到插件目录

3. 重启 Wireshark

### 验证安装

1. 打开 Wireshark
2. 打开 **Lua Console**（工具 → Lua Console）
3. 应该看到以下输出：
   ```
   [RSIC] 插件加载成功！
   [RSIC] 正在注册端口: 10020,10030,10090
   [RSIC] 已注册端口: 10020
   [RSIC] 已注册端口: 10030
   [RSIC] 已注册端口: 10090
   [RSIC] 主解析函数已注册
   ```
4. 菜单栏选择 **编辑 → 首选项 → 协议 → RSIC**
5. 如果能看到 RSIC 协议配置项，说明安装成功

## 使用说明

### 配置端口

1. 打开 Wireshark
2. 菜单栏选择 **编辑 → 首选项 → 协议 → RSIC**
3. 在 **端口号(s)** 字段输入要监听的端口（默认: 10020,10030,10090）
4. 多个端口用英文逗号分隔
5. 修改后会自动重新注册端口

### 选择线路

1. 在 RSIC 协议首选项中
2. 在 **项目选择** 下拉框中选择对应线路：
   - **广州18&22&11号线**
   - **广州10&12号线**

### 抓包分析

1. 在 Wireshark 中开始抓包
2. 过滤器中使用: `tcp.port == 10020` 或其他配置的端口
3. 选择 RSIC 协议的数据包
4. 在详情面板中查看解析结果

### 协议字段说明

#### 协议头部（9字节）

| 偏移 | 长度 | 字段名 | 说明 |
|------|------|--------|------|
| 0 | 2 | 数据头标识 | 固定值 |
| 2 | 1 | 分包总数 | 该消息的总分包数 |
| 3 | 1 | 当前分包序号 | 当前分包的序号（从1开始） |
| 4 | 2 | 数据体长度 | 数据部分的字节长度 |
| 6 | 1 | 设备主备状态 | 1=主用, 2=备用 |
| 7 | N | 数据类型 | 仅首包显示（见数据类型表） |

#### 协议尾部（2字节）

| 偏移 | 长度 | 字段名 | 说明 |
|------|------|--------|------|
| -2 | 2 | 数据尾标识 | 固定值 |

#### 列车信息结构

**广州18/11/22号线列车数据（基础26字节 + 变长部分）**

| 字段 | 长度 | 说明 |
|------|------|------|
| 车组号长度 | 1 | 变长字段长度 |
| 车组号 | N | 车组编号 |
| 车次号长度 | 1 | 变长字段长度 |
| 车次号 | N | 车次编号 |
| 运行方向 | 1 | 1=上行, 2=下行 |
| 中心站ID | 2 | 中心站标识 |
| 前方分区ID | 2 | 前方分区标识 |
| 前方分区偏移 | 4 | 偏移量（厘米） |
| 紧急制动 | 1 | 0=正常, 1=紧急制动 |
| 封锁状态 | 1 | 0=未封锁, 1=已封锁 |
| 休眠状态 | 2 | 休眠状态码 |
| 工作信息 | 1 | 工作状态信息 |
| 自动驾驶 | 1 | 0=手动, 1=自动 |
| 司机请求 | 1 | 司机请求状态 |
| CAM请求 | 1 | CAM请求状态 |
| 清空站台ID | 4 | 清空站台标识 |
| 超重状态 | 1 | 0=正常, 1=超重 |
| 越站模式 | 1 | 0=正常, 1=越站 |
| 火灾状态 | 1 | 0=正常, 1=火灾 |

**广州10/12号线额外字段（+15字节）**

| 字段 | 长度 | 说明 |
|------|------|------|
| 驾驶模式 | 1 | 驾驶模式 |
| 停车精度 | 2 | 停车精度值 |
| 列车速度 | 2 | 列车速度 |
| 预留 | 10 | 保留字段 |

## 优化内容

### v1.0.1#20250130-1 (2025-01-30)

#### 1. 修复兼容性问题

**修复 ByteArray:clear() 不兼容**
- **问题**: 旧版 Wireshark 不支持 `ByteArray:clear()` 方法
- **解决**: 使用 `ByteArray.new()` 重新创建替代清空操作

**修复 tohex() 方法不兼容**
- **问题**: 旧版 Wireshark 的 `TvbRange:tohex()` 方法不存在
- **解决**: 创建兼容函数 `to_hex_string()`，支持新旧版本
- **影响范围**: 17处调用（心跳、预留字段、NID_PSD、色码等）

**修复 pinfo.private 不兼容**
- **问题**: 部分版本的 Wireshark 不支持 `pinfo.private`
- **解决**: 改用全局 `session_cache` 表，使用会话 key 作为索引

#### 2. 优化代码格式化

- 统一缩进和空格（4空格缩进）
- 添加清晰的分隔线注释（===）
- 分组组织代码结构
- 统一命名风格
- 改进函数注释（使用 LuaDoc 格式）

#### 3. 中文化显示

**协议字段中文化**
- `data_head` → "数据头标识"
- `total` → "分包总数"
- `index` → "当前分包序号"
- `data_len` → "数据体长度"
- `dev_status` → "设备主备状态"
- 所有业务字段都添加了中文描述

**枚举值映射中文化**
- `haStatusStr`: `[1] = "主用", [2] = "备用"`
- `directionStr`: `[1] = "上行", [2] = "下行"`
- `blockStr`: `[0] = "未封锁", [1] = "已封锁"`
- `stopTrainTypeStr`: `[0] = "停车", [1] = "通过"`
- `currentStatusStr`: `[0] = "未进站", [1] = "进站中", [2] = "站内停车"`
- `holdTrainStr`: `[0] = "不扣车", [1] = "扣车"`
- `fastTrainFlagStr`: `[0] = "普通车", [1] = "快车"`
- `clearTrainFlagStr`: `[0] = "不清客", [1] = "清客"`
- `powerStatusStr`: `[0] = "未知", [1] = "供电", [2] = "断电"`
- `fireStatusStr`: `[0] = "未知", [1] = "火灾", [2] = "无火灾"`

**显示文本优化**
- "Data: heartbeat, len:123" → "业务数据: 心跳信息 | 长度: 123"
- "trainId:1 ,len:50" → "列车1 | 数据长度: 50"
- 所有显示都使用中文和统一格式

#### 4. 改进错误处理

- 增强边界安全检查
- 使用 `add_expert_info` 标记解析错误
- 在 Lua Console 输出详细错误信息
- 自动处理无效分包

#### 5. 性能优化

- 优化会话缓存管理
- 减少不必要的字符串拼接
- 提高分包重组效率

### v1.0.0#20241107-1 (2024-11-07)

#### 1. 修复全局缓冲区问题
- **问题**: 使用全局 `tmp_buf` 导致多连接数据串扰
- **解决**: 改用会话级独立缓存

#### 2. 修复函数名拼写错误
- `hearbeat_data` → `heartbeat_data`
- `firsLast_data` → `firstLast_data`
- `firsLastRequest_data` → `firstLastRequest_data`

#### 3. 增强边界安全检查
- 分包解析前验证缓冲区长度
- 循环解析时检查剩余数据长度

#### 4. 改进时间格式化
- 使用 `string.format` 补零
- 添加数值范围校验
- 格式: `YYYY-MM-DD HH:MM:SS`

#### 5. 修复端口注册问题
- 使用 `prefs_changed` 方法监听首选项变化
- 移除不兼容的 API 调用

#### 6. 改进代码结构
- 移除 `goto` 语句
- 添加常量定义集中管理
- 统一错误处理逻辑

## 常见问题

### Q1: 插件加载失败，显示 Lua Error

**可能原因**:
1. Wireshark 版本过低（建议 3.0+）
2. 插件文件损坏
3. Lua 语法错误

**解决方案**:
1. 检查 Wireshark 版本是否支持 Lua 插件
2. 确认插件文件放在正确的目录
3. 打开 Lua Console 查看具体错误信息
4. 检查插件文件编码（应为 UTF-8）

### Q2: 数据包没有解析为 RSIC 协议

**可能原因**:
1. 端口配置不正确
2. 插件未正确注册端口
3. 数据不是 RSIC 协议

**解决方案**:
1. 检查端口配置是否正确
2. 在 Lua Console 中查看端口注册信息
3. 确认数据确实是 RSIC 协议
4. 尝试重启 Wireshark
5. 在 Lua Console 中手动注册端口：
   ```lua
   local tcp_table = DissectorTable.get("tcp.port")
   tcp_table:add(10090, Dissector.get("rsic"))
   ```

### Q3: 中文显示乱码

**说明**: 车组号、车次号等字段使用 GBK 编码，部分特殊字符可能显示乱码，属于正常现象。

### Q4: 分包不完整，显示"拼接中"

**说明**: 插件会自动处理分包重组，如果显示"拼接中"说明正在等待后续包。完整包会显示"拼接后总数据长度"。

### Q5: 如何查看解析错误

**解决方案**:
1. 在 Wireshark 底部控制台查看错误日志
2. 在详情面板中查看红色标记的错误节点
3. 检查 Wireshark 的 **专家信息** 窗口
4. Lua Console 会输出详细的错误信息

### Q6: 修改端口配置后不生效

**解决方案**:
1. 关闭并重新打开首选项窗口
2. 重启 Wireshark
3. 在 Lua Console 中确认端口重新注册：
   ```
   [RSIC] 首选项已更改，重新注册端口
   ```

### Q7: 出现 "No such method/field" 错误

**说明**: 这是 Wireshark 版本兼容性问题，插件已经处理了大部分兼容性问题。

**解决方案**:
1. 升级到最新版本的 Wireshark
2. 检查具体的错误信息
3. 如果是新的兼容性问题，请报告给开发者

## 技术支持

### 问题反馈

如遇到问题，请提供以下信息：
- Wireshark 版本号
- 操作系统版本
- Lua Console 的完整输出
- 错误信息截图
- 抓包文件（脱敏后，如有必要）

### 联系方式

- **开发者**: yearing
- **更新时间**: 2025-01-30

### 协议分析

如需分析新的协议字段或数据类型，请参考现有代码结构进行扩展。

## 开发说明

### 代码结构

```
rsic_protocol.lua
├── 插件头部注释
├── 协议定义
├── 首选项配置
├── 协议字段定义（ProtoField）
├── 枚举值映射（中文化）
├── 工具函数
│   ├── to_hex_string（十六进制转换）
│   ├── getTime（时间格式化）
│   └── get_session_key（会话标识）
├── 核心解析函数
│   ├── full_packet_dissactor（分包处理）
│   └── rsic_protocol.dissector（主入口）
└── 业务数据解析函数
    ├── heartbeat_data（心跳）
    ├── train_data（列车信息）
    ├── gzl10_train_data（10号线列车）
    ├── plan_data（计划信息）
    ├── firstLast_data（首末班）
    ├── trainDoor_data（车门）
    ├── platformDoor_data（站台门）
    ├── stationYard_data（站场）
    ├── power_data（供电臂）
    ├── firstLastRequest_data（首末班申请）
    ├── stationPlan_data（场段计划）
    ├── fire_data（车站火灾）
    ├── zonefire_data（区间火灾）
    └── zonewater_data（区间水灾）
```

### 添加新数据类型

1. 在 `dataTypeStr` 中添加类型定义
2. 创建对应的解析函数（命名格式：`xxx_data`）
3. 在 `full_packet_dissactor` 中调用新函数
4. 在枚举值映射中添加相应的状态值

### 代码规范

- 使用 4 空格缩进
- 添加详细的注释说明
- 函数使用 LuaDoc 格式注释
- 所有显示文本使用中文
- 添加边界检查防止缓冲区越界
- 使用 `add_expert_info` 标记错误
- 保持函数命名一致性

### 兼容性说明

插件已经针对不同版本的 Wireshark 进行了兼容性处理：
- `ByteArray:clear()` → 使用 `ByteArray.new()` 重新创建
- `TvbRange:tohex()` → 使用兼容函数 `to_hex_string()`
- `pinfo.private` → 使用全局 `session_cache` 表
- `DissectorTable:remove_all_for_dissector()` → 使用 `prefs_changed`

## 更新日志

### v1.0.1#20250130-1 (2025-01-30)
- ✅ 修复 ByteArray:clear() 方法兼容性问题
- ✅ 修复 TvbRange:tohex() 方法兼容性问题
- ✅ 修复 pinfo.private 兼容性问题
- ✅ 优化代码格式化
- ✅ 添加全面的中文显示
- ✅ 改进错误处理和提示
- ✅ 优化会话缓存管理
- ✅ 完善文档说明

### v1.0.0#20241107-1 (2024-11-07)
- ✅ 修复全局缓冲区导致的多连接数据串扰问题
- ✅ 修复函数名拼写错误
- ✅ 增强边界安全检查
- ✅ 改进时间格式化输出
- ✅ 修复端口注册兼容性问题
- ✅ 优化代码结构，移除 goto 语句

### v1.0.0 (初始版本)
- ✅ 支持广州18/11/22号线协议解析
- ✅ 支持广州10/12号线协议解析
- ✅ 实现TCP分包自动重组
- ✅ 支持所有业务数据类型解析

## 许可证

本插件为内部使用工具，版权归广州地铁所有。

---

**最后更新**: 2025-01-30  
**维护者**: yearing  
**插件版本**: 1.0.1#20250130-1